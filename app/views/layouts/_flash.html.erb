<% flash.each do |flash_type, message| %>
  <div class="flex fixed justify-center w-full">
    <div class="bg-neutral-800 text-white px-6 py-1 my-4 rounded-full animate-appear-then-fade cursor-pointer">
      <%= message %>
    </div>
  </div>
<% end %>