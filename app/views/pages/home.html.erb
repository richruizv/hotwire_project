<main class=" text-center">
  <h1 class="mb-4">Quote editor</h1>
  <p class="mb-2">A blazing fast quote editor built with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View quotes", quotes_path, class: "btn btn-rose" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn-green" %>
  <% end %>

  <div class="mt-10">
    <h3 class="text-1xl text-center mb-4 "> 
      Made with love and
      <span class="inline-block w-4 text-red-500	"><%=show_svg('hotrails.svg') %></span>
      Hotrails 
    </h3>
  </div>
</main>